// @slift/ui — public entry point.
// NumberRollDigit ships alongside (NumberRoll imports it) but is an internal
// detail, so only NumberRoll is exported here.
export { default as NumberRoll } from './NumberRoll.svelte'
